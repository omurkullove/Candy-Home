import React from 'react';
import PackageIdComponent from "../../components/package/PackageIdComponent/PackageIdComponent";

const PackageId = () => {
    return (
        <PackageIdComponent />
    );
};

export default PackageId;